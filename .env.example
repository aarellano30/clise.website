# Keystatic CMS Configuration
# In development: uses 'local' storage automatically (no env vars needed)
# In production: uses 'github' storage with the vars below

# GitHub storage mode (set these in Netlify env vars for production)
PUBLIC_KEYSTATIC_GITHUB_REPO_OWNER=your-org
PUBLIC_KEYSTATIC_GITHUB_REPO_NAME=clisev3
KEYSTATIC_GITHUB_CLIENT_ID=
KEYSTATIC_GITHUB_CLIENT_SECRET=
KEYSTATIC_SECRET=
