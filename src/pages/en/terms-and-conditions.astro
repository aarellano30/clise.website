---
import Layout from '../../layouts/Layout.astro';
import TermsPage from '../../components/TermsPage.astro';
import { useTranslations } from '../../i18n/utils';

const t = useTranslations('en');
---

<Layout title={`${t('terms.title')} - ${t('footer.company')}`}>
	<TermsPage />
</Layout>
